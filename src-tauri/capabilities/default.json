{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window of Tetrix.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "store:default"
  ]
}