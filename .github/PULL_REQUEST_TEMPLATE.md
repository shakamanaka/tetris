## What changed

Describe the user-visible behavior first.

## Why

Explain the problem or decision this change addresses.

## Validation

- [ ] `npm run format:check`
- [ ] `npm run typecheck`
- [ ] `npm test`
- [ ] `npm run lint`
- [ ] `npm run build`
- [ ] `cargo fmt --manifest-path src-tauri/Cargo.toml --all -- --check` (when native code or configuration changed)
- [ ] `cargo check --manifest-path src-tauri/Cargo.toml` (when native code or configuration changed)

## Screenshots or recording

Include visual evidence when the UI changes.
